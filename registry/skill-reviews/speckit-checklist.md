## Skill Review: speckit.checklist

**Source**: External — `https://github.com/github/spec-kit` (`templates/commands/checklist.md`)
**Skill type**: Script-backed (bash)
**Date reviewed**: 2026-03-14
**Reviewed by**: Joshua Alexander Clement + claude-sonnet-4-6
**Risk classification**: Low–Medium
**Dependency chain**: spec-kit must be installed in target project

---

### Static Analysis

**§2.0 File Inventory**

| File | Category |
|---|---|
| `templates/commands/checklist.md` | Skill definition |
| `scripts/bash/check-prerequisites.sh` | Bash script |
| `scripts/bash/common.sh` | Bash script (sourced) |

**§2.1 Tool Inventory**

| Tool | Risk | Notes |
|---|---|---|
| `Bash` | Low | Runs `check-prerequisites.sh --json` |
| `Read` | Low | Loads spec.md, plan.md, tasks.md |
| `Write` | Low–Medium | Creates/appends `FEATURE_DIR/checklists/[domain].md` |

**§2.2 File Scope Analysis**

Reads: spec.md, plan.md, tasks.md (all within FEATURE_DIR). Writes: `FEATURE_DIR/checklists/[domain].md` — creates directory if absent, appends if file exists. Write scope is bounded to FEATURE_DIR. No writes outside feature directory. No user-controlled path injection — filename derived from domain keyword extracted from request.

**§2.3 Network Scope Analysis**

No network calls. check-prerequisites.sh is local only.

**§2.4 Prompt Injection Scan**

No override instructions or bypass language. `$ARGUMENTS` is used as context input but does not control file paths or script arguments. Checklist generation is bounded by the command's explicit rules.

**§2.5 Hook and Injection Analysis**

No hooks, MCP config, agents/ directory, or settings files. check-prerequisites.sh sources local common.sh only.

---

### Script Analysis

**§2.6.1 Shell Script**

Same check-prerequisites.sh as speckit.analyze (see speckit-analyze.md §2.6.1). No writes — path resolution only.

**§2.6.2 File Write Bounds**

Writes are bounded to `FEATURE_DIR/checklists/`. Checklist filename is `[domain].md` where domain is a short keyword (e.g., `ux.md`, `security.md`). The command explicitly states "never delete or replace existing checklist content — always preserve and append." No path traversal possible as domain keyword is generated by the model, not taken directly from raw user input.

**§2.6.3 spec-kit Compatibility Prerequisite**

Requires spec-kit git-branch naming convention and check-prerequisites.sh. See speckit-analyze.md §2.6.3.

---

### Constitutional Review

| Part | Check | Result |
|---|---|---|
| Part 1, Amendment 5 | No oversight bypass | PASS |
| Part 5, Amendment 1 | No deception | PASS |
| Part 5, Amendment 4 | No unauthorized data handling | PASS — writes only checklist content to local feature dir |
| Part 5, Amendment 5 | Outputs attributable | PASS |
| Part 3, Amendment 1 | No authority overreach | PASS — writes only to bounded feature dir |

---

### Execution Test

**Phase 4 — Deferred**: spec-kit not installed in LLM-Quickstart. Required before first use in a spec-kit-enabled project.

Verify: checklist file is created only within FEATURE_DIR/checklists/; no writes outside that boundary; append behavior preserves existing content.

---

### Decision

**APPROVED WITH CONDITIONS**

**Conditions:**
1. Target project must have spec-kit installed
2. Phase 4 execution test required before first use
3. `FEATURE_DIR` must resolve to a path within the project (guaranteed by spec-kit's branch naming convention)
4. Checklist content is human-reviewable before passing to `/speckit.implement`

**Approved by:** Joshua Alexander Clement + claude-sonnet-4-6, 2026-03-14
